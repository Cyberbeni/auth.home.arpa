{
	// Default is "SSO"
	"title": "My Home Server SSO",
	// Default is false
	"session_cookie": true,
	// Default is 23 hours when session_cookie == true, 30 days otherwise
	"session_duration": 82800,
	// Omit to generate a new signing key at every app start
	"secret": "secret"
}